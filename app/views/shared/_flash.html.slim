- if flash
  - flash.each do |key, value|
    div class='#{flash_div_class(key)}' role='alert'
      = value
      button.close type='button' data-dismiss='alert' aria-label='Close'
        span aria-hidden='true'
          | &times;
